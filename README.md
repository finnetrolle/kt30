# Technical Specification Analyzer

Приложение для анализа технических заданий из Microsoft Word файлов и автоматического создания Work Breakdown Structure (WBS) с использованием OpenAI API.

## Возможности

- 📤 Загрузка документов Word (.doc, .docx)
- 🤖 Автоматический анализ технического задания с помощью ИИ
- 📊 Генерация детальной структуры работ (WBS)
- ⏱️ Оценка трудозатрат для каждой задачи
- ⚠️ Выявление рисков проекта
- 💡 Рекомендации по реализации
- 📥 Экспорт результатов в JSON

## Требования

- Python 3.8+
- OpenAI API ключ

## Установка

1. Клонируйте репозиторий или скопируйте файлы проекта

2. Создайте виртуальное окружение:
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл конфигурации:
```bash
cp .env.example .env
```

5. Отредактируйте `.env` файл и укажите ваш OpenAI API ключ:
```
OPENAI_API_KEY=your-openai-api-key-here
```

## Конфигурация

Основные параметры конфигурации в файле `.env`:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `SECRET_KEY` | Секретный ключ Flask | - |
| `DEBUG` | Режим отладки | False |
| `OPENAI_API_KEY` | API ключ OpenAI | - |
| `OPENAI_API_BASE` | Базовый URL API | https://api.openai.com/v1 |
| `OPENAI_MODEL` | Модель для использования | gpt-4 |
| `UPLOAD_FOLDER` | Папка для загрузок | uploads |
| `MAX_CONTENT_LENGTH` | Макс. размер файла (байт) | 16777216 (16MB) |

## Запуск

### Режим разработки

```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

### Продакшн (с Gunicorn)

```bash
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

## Использование

1. Откройте веб-браузер и перейдите по адресу http://localhost:5000
2. Загрузите документ Word с техническим заданием
3. Нажмите кнопку "Анализировать"
4. Дождитесь обработки (обычно 10-30 секунд)
5. Просмотрите результаты анализа:
   - Информация о проекте
   - Фазы и рабочие пакеты WBS
   - Риски проекта
   - Предположения
   - Рекомендации
6. Экспортируйте результаты в JSON при необходимости

## Структура проекта

```
.
├── app.py                 # Основное Flask приложение
├── config.py              # Конфигурация
├── document_parser.py     # Парсер Word документов
├── openai_client.py       # Клиент OpenAI API
├── requirements.txt       # Зависимости Python
├── wbs_template.md        # Шаблон структуры WBS для промпта
├── .env.example           # Пример конфигурации
├── templates/
│   ├── index.html         # Страница загрузки
│   ├── results.html       # Страница результатов
│   └── error.html         # Страница ошибки
├── static/
│   ├── css/
│   │   └── style.css      # Стили
│   └── js/
│       ├── main.js        # JavaScript главной страницы
│       └── results.js     # JavaScript страницы результатов
└── uploads/               # Папка для загруженных файлов
```

## Формат WBS

Приложение генерирует WBS в следующей структуре:

- **Информация о проекте** - название, описание, длительность, сложность
- **Фазы проекта**:
  1. Инициация и планирование
  2. Проектирование
  3. Разработка
  4. Тестирование
  5. Развертывание
  6. Поддержка и сопровождение
- **Рабочие пакеты** - детализация работ в каждой фазе
- **Задачи** - конкретные работы с оценкой трудозатрат
- **Риски** - идентификация и митигация
- **Рекомендации** - советы по реализации

## API Endpoints

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/` | GET | Главная страница с формой загрузки |
| `/upload` | POST | Загрузка и анализ документа |
| `/results/<id>` | GET | Просмотр результатов анализа |
| `/api/results/<id>` | GET | Получение результатов в JSON |
| `/health` | GET | Health check |

## Использование собственного OpenAI-совместимого API

Вы можете использовать любой OpenAI-совместимый API, указав его URL в параметре `OPENAI_API_BASE`:

```env
OPENAI_API_BASE=https://your-custom-api.com/v1
OPENAI_API_KEY=your-api-key
OPENAI_MODEL=your-model-name
```

## Лицензия

MIT License

## Автор

Technical Specification Analyzer © 2024
